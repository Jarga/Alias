import Common = require("../../../common/lib/index");
import Google = require("../application/Google/Pages/GoogleSession");

describe("google search", function () {
    var config = new Common.DefaultConfiguration();

    it("can be controlled", function() {

        expect(true);
    });
});
